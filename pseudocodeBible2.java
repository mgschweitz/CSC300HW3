
public class pseudocodeBible2 
{
	/*relevant pseudo code:
	 * 
	 * variables:
	 * 	population(long)
	 * 	disciples(long)
	 * 	catechists(long)
	 * 	time(int)
	 * 	array of generations
	 * 	list of catechist generations
	 * 
	 * birth at every 10 years
	 * death at every 18 years
	 * trainer at every 6 years
	 * 
	 * generation:
	 * 	population
	 * 		# of disciples in generation
	 * 		# of catechists in generation
	 * 	bday
	 * 	pday
	 * 	dday
	 * 
	 * 
	 * 
	 * each loop is a year
	 * step one of the loop is the # of catechsits goes up by the amount of truedisciples that are over 18 (TD&TDay) 
	 * next step is TD+=catechists*traintime 
	 * die
	 * 
	 * 
	 * 
	 * gen[i]
	 * 	if gen[i+1]=true
	 * 		train gen[i+1]
	 * 	
	 * 	if else gen[i+1]=false or full
	 * 		train gen[i]
	 */
}
